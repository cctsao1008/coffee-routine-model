# Tiny Change-Point Result ✂️🐣☕

The first reproducible synthetic boundary check uses:

```text
before world : cozy-normal-year
after world  : sleepy-reply-season
days         : 180
true boundary: day 91
min segment  : 24
seed         : 20260908
change prior : 0.20
```

Latest CI result:

```text
true boundary             = day 91
best detected boundary    = day 93
absolute timing error     = 2 days
P(change | clues)         = 0.968
stable-control P(change)  = 0.395
```

With the default detection threshold of `0.80`:

```text
known persistent shift -> DETECTED
stable control         -> NOT DETECTED
```

This is a synthetic architecture check, not evidence about any real person or real routine.

Tiny scissors found the new lane without cutting the stable control in half. ✂️🌱XD
